@model ProductsListViewModel
@foreach (var product in Model.Products)
{
    <div>
        <h3>@product.Name - @product.Price.ToString("C2")</h3>
        <p>@product.Description</p>
    </div>
}
<div page-model="@Model.PagingInfo" page-action="List"></div>